<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fish Caught</title>
</head>
<body>
    <h1>Fish Caught</h1>
    <ul>
        <% fish_caught.forEach(fish => { %>
            <li>
                <%= fish.text %>
                -
                <%= fish.isOpenSeason ? 'openSeason' : 'notOpenSeason' %>
                -
                <%= fish.caught ? 'caught' : 'not caught' %>
            </li>
        <% }) %>
    </ul>
</body>
</html>